#!/bin/bash

QUERY="$@"
[ -z "$QUERY" ] && exit 0

FULL_QUERY="Answer in as little words as possible, with no additional delimiting punctuation.  Do not ask clarifying questions.  Print your answer and do not write to file.  Assume language is bash unless specified otherwise: $QUERY"

claude -p --model haiku  "${FULL_QUERY}"